This is the code for the DP-SGD experiments of our paper.

Required packages: numpy, scipy, sklearn, matplotlib, typer, pathlib

To run a simple example on a single run, use:
python main.py

For more options, check:
python main.py --help
